# alienvox_tts — standalone TTS SDK, extracted from python_app.
# Same version floors/ceilings as python_app/requirements.txt, kept
# consistent even though this library only ships the Kokoro engine for
# now — makes adding more engines later (which do need torch/transformers
# directly, unlike Kokoro) a version-pin-free addition.

torch>=2.0.0
transformers>=4.30.0
huggingface_hub[hf_xet]>=0.20.0
numpy>=1.24.0,<2.0.0

kokoro>=0.9.0

soundfile>=0.12.1
sounddevice>=0.4.6
